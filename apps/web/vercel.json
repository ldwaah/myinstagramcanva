{
  "buildCommand": "cd ../.. && npm run db:generate && npm run build -w web",
  "installCommand": "cd ../.. && npm install && npm install --include=optional -w web",
  "framework": "nextjs",
  "regions": ["lhr1"]
}
