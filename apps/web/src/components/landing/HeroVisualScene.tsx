/** Subtle branded gradient mesh — aurora/grain provided by parent sections */
export function HeroVisualScene() {
  return (
    <div className="hero-scene" aria-hidden>
      <div className="hero-scene__mesh" />
    </div>
  );
}
