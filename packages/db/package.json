{
  "name": "@mic/db",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "prepare-schema": "node scripts/prepare-schema.mjs",
    "generate": "npm run prepare-schema && prisma generate",
    "push": "npm run prepare-schema && prisma db push",
    "studio": "npm run prepare-schema && prisma studio",
    "build": "npm run generate"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0"
  },
  "devDependencies": {
    "prisma": "^6.9.0",
    "typescript": "^5.8.3"
  }
}
